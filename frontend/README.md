# Overcome — frontend

React + Vite + TypeScript + Tailwind + shadcn/ui.

## Local development

```bash
cd frontend
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

Set `VITE_API_URL` / `VITE_API_BASE_URL` as needed for your backend.
